# DACON 236749 already provides torch 2.7.1+cu128, torchaudio, numpy, pandas,
# scipy, librosa, soundfile, demucs and tqdm. Do not re-install evaluator-pinned
# packages within the 10-minute installation limit.
# DF_Arena is dynamic INT8 and runs entirely on the official server's 6 vCPU.
# The CPU wheel is much smaller/faster to install than onnxruntime-gpu and
# avoids CPU/GPU execution-provider partitioning overhead.
onnxruntime==1.22.0
