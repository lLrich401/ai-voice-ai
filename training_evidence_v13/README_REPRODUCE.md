# V13_FAILED_PILOT historical evidence

Status: **V13_FAILED_PILOT — not production evidence**.

This directory contains manifests, hashes, configuration, and generation tooling only. It intentionally excludes audio and hidden DACON test data. V13 model training was not started because the pre-registered source-shortcut AUC gate failed. V13B evidence, if training is eventually authorized, must be written to a separate `training_evidence_v13b/` directory. FINAL_HOLDOUT_V13 remains historical and sealed.
