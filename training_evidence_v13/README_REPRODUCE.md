# V13 training evidence

This directory contains manifests, hashes, configuration, and generation tooling only. It intentionally excludes audio and hidden DACON test data. V13 model training was not started because the pre-registered source-shortcut AUC gate failed. Rebuild the pilot, run the shortcut audit, and train only after the audit passes. FINAL_HOLDOUT_V13 remains sealed until a candidate is locked.
